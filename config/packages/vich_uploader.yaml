vich_uploader:
    db_driver: orm

    mappings:
        news_image:
            uri_prefix:         '%app.path.news_image%'
            upload_destination: '%kernel.project_dir%/public/%app.path.news_image%'
